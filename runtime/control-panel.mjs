const CSP_NONCE_PATTERN = /^[A-Za-z0-9+/]{24}$/;

export function controlPanelHtml(nonce) {
  if (typeof nonce !== 'string' || !CSP_NONCE_PATTERN.test(nonce)) throw new TypeError('nonce must be base64');
  return `<!doctype html>
<html lang="zh-CN">
<head>
<meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>OpenClaw Workbench</title>
<style>
:root{color-scheme:dark;--bg:#090d13;--surface:#111821;--surface-2:#182331;--line:#263445;--text:#eef5ff;--muted:#91a1b5;--blue:#5b9cff;--cyan:#57d7d4;--green:#45d483;--yellow:#f4bd62;--red:#ff6b75;--shadow:0 18px 60px #0008}*{box-sizing:border-box}body{margin:0;background:radial-gradient(900px 520px at 88% -10%,#18345e 0,transparent 62%),linear-gradient(145deg,#080c12,#0d131c 55%,#0a1018);color:var(--text);font:14px/1.5 Inter,ui-sans-serif,system-ui,-apple-system,"Segoe UI",sans-serif;min-height:100vh}button,input,textarea,select{font:inherit;color:inherit}button{border:1px solid var(--line);background:var(--surface-2);border-radius:9px;padding:9px 13px;cursor:pointer;transition:.18s ease}button:hover:not(:disabled){border-color:var(--blue);transform:translateY(-1px)}button:disabled{opacity:.45;cursor:not-allowed}.primary{background:linear-gradient(135deg,#3479e9,#2660c5);border-color:#5b9cff;box-shadow:0 7px 22px #1d5cc255}.danger{color:#ff9da4;border-color:#74333d;background:#29171c}.ghost{background:transparent}.eyebrow{color:var(--muted);font-size:11px;font-weight:700;letter-spacing:.13em;text-transform:uppercase}.muted,.small{color:var(--muted);font-size:12px}.appbar{height:72px;border-bottom:1px solid #213044;background:#0b1119cc;backdrop-filter:blur(18px);display:flex;align-items:center;padding:0 clamp(18px,4vw,54px);gap:13px;position:sticky;top:0;z-index:4}.mark{width:34px;height:34px;border-radius:11px;display:grid;place-items:center;background:linear-gradient(135deg,#5b9cff,#57d7d4);color:#07111e;font-weight:900;box-shadow:0 0 24px #4285f455}.brand{font-size:17px;font-weight:800;letter-spacing:-.02em}.tag{color:var(--muted);font-size:13px}.auth{margin-left:auto;display:flex;gap:7px}.auth input{width:180px;border:1px solid var(--line);border-radius:8px;background:#0a1119;padding:8px 10px;outline:0}.auth input:focus{border-color:var(--blue)}.connection{display:flex;align-items:center;gap:8px;color:var(--muted);font-size:12px}.light{width:8px;height:8px;border-radius:50%;background:var(--yellow);box-shadow:0 0 10px currentColor}.light.ok{background:var(--green)}.light.bad{background:var(--red)}main{width:min(1400px,100%);margin:0 auto;padding:28px clamp(16px,4vw,54px) 48px;display:grid;grid-template-columns:280px minmax(0,1fr);gap:20px}.stack{display:grid;gap:16px;align-content:start}.card{border:1px solid var(--line);border-radius:15px;background:linear-gradient(145deg,#131c27f2,#0f1721f2);box-shadow:var(--shadow);padding:18px}.workspace{padding:20px}.workspace h1{font-size:24px;margin:7px 0 4px;letter-spacing:-.04em}.workspace p{margin:0 0 18px;color:var(--muted);font-size:13px}.root{padding:10px;border:1px solid #25364a;border-radius:9px;background:#0b121b;color:#a9bed7;word-break:break-all;font-size:12px}.metric{display:flex;justify-content:space-between;padding:11px 0;border-bottom:1px solid #203044;color:var(--muted)}.metric:last-child{border-bottom:0}.metric b{color:var(--text)}.mode-list{display:grid;gap:8px;margin-top:12px}.mode{display:flex;align-items:center;gap:10px;text-align:left}.mode i{width:7px;height:7px;border-radius:50%;background:#65778d}.mode.active{background:#14315b;border-color:#4e8fe8;color:#fff}.mode.active i{background:var(--cyan);box-shadow:0 0 12px var(--cyan)}.content{min-width:0;display:grid;gap:16px}.hero{padding:22px 22px 18px;display:flex;justify-content:space-between;gap:16px;align-items:flex-start;background:linear-gradient(135deg,#17283d,#121d2b 60%,#13282c)}.hero h2{margin:5px 0 4px;font-size:22px;letter-spacing:-.03em}.hero p{margin:0;color:#aec0d5}.hero-actions{display:flex;gap:8px;flex-wrap:wrap}.session-id{font-family:ui-monospace,monospace;color:var(--cyan);font-size:11px;word-break:break-all}.messages{height:430px;overflow:auto;display:flex;flex-direction:column;gap:12px;padding:4px 2px}.messages::-webkit-scrollbar{width:8px}.messages::-webkit-scrollbar-thumb{background:#31445a;border-radius:10px}.msg{max-width:86%;padding:12px 14px;border:1px solid var(--line);border-radius:13px;white-space:pre-wrap;overflow-wrap:anywhere}.msg.user{align-self:flex-end;background:#15345c;border-color:#2e6cb8;border-bottom-right-radius:4px}.msg.assistant{background:#0f1720;border-bottom-left-radius:4px}.msg b{display:block;color:var(--muted);font-size:11px;margin-bottom:4px}.empty{margin:auto;text-align:center;color:var(--muted);padding:40px}.composer{border-top:1px solid #213044;padding-top:15px}.field{width:100%;border:1px solid var(--line);border-radius:11px;background:#0a1119;padding:13px;resize:vertical;min-height:90px;outline:0}.field:focus{border-color:var(--blue);box-shadow:0 0 0 3px #3479e922}.toolbar{display:flex;align-items:center;gap:9px;margin-top:10px}.toolbar .spacer{flex:1}.kbd{border:1px solid #3a4a5c;background:#1b2634;border-radius:5px;padding:2px 6px;color:#b9c9da;font-size:11px}.split{display:grid;grid-template-columns:1fr 1fr;gap:16px}.row{display:flex;align-items:center;justify-content:space-between;gap:10px}.row h3{margin:5px 0;font-size:15px}.plan-output,.proposal,.event-log{margin-top:13px;border:1px solid #223247;border-radius:10px;background:#0b121a;padding:12px;min-height:52px;white-space:pre-wrap;overflow:auto;max-height:260px;font-family:ui-monospace,monospace;font-size:12px;color:#c4d5e8}.plan-history{display:grid;gap:8px;margin-top:12px}.plan-history details{border:1px solid #223247;border-radius:9px;background:#0b121a;padding:9px 11px}.plan-history summary{cursor:pointer;color:#c4d5e8}.plan-history pre{margin:9px 0 0;white-space:pre-wrap;overflow:auto;max-height:220px;color:#9fb4ca;font:12px ui-monospace,monospace}.proposal{border-left:3px solid var(--yellow)}.event-log{max-height:160px;color:#9fb4ca}.event{padding:5px 0;border-bottom:1px solid #1b2939}.event:last-child{border:0}.event time{color:#687c93;margin-right:8px}.hint{margin:9px 0 0;color:var(--muted);font-size:12px}.debate-options{display:grid;grid-template-columns:auto 1fr 1fr;gap:9px;align-items:center;margin-top:12px}.debate-options input[type=text]{width:100%;border:1px solid var(--line);border-radius:8px;background:#0a1119;padding:8px 10px;outline:0}.debate-options label{color:var(--muted);font-size:12px}.stage-list{display:grid;grid-template-columns:repeat(4,1fr);gap:6px;margin-top:12px}.stage{border:1px solid #263445;border-radius:8px;padding:7px;color:var(--muted);font-size:11px}.stage.active{border-color:var(--blue);color:var(--text)}.stage.done{border-color:var(--green);color:var(--green)}.stage.failed{border-color:var(--red);color:var(--red)}.toast{position:fixed;right:22px;bottom:22px;max-width:min(420px,calc(100vw - 44px));padding:12px 15px;border:1px solid #3f5875;background:#142233f5;border-radius:10px;box-shadow:var(--shadow);z-index:8}.toast.bad{border-color:#713944;color:#ffb1b7}.hidden{display:none!important}.workspace-tools{display:grid;grid-template-columns:260px minmax(0,1fr);gap:12px}.tree{max-height:330px;overflow:auto;border:1px solid #223247;border-radius:10px;background:#0b121a;padding:8px}.tree-item{display:block;width:100%;padding:5px 7px;border:0;border-radius:6px;cursor:pointer;color:#b8c9dc;font:12px ui-monospace,monospace;text-align:left}.tree-item:hover,.tree-item:focus-visible{background:#17283d;color:#fff;outline:2px solid var(--blue);outline-offset:1px}.tree-item.selected{background:#14315b;color:#fff;box-shadow:inset 3px 0 0 var(--cyan)}.tree-item.dir{color:#7fd8d5}.preview{min-height:330px;max-height:420px;overflow:auto;margin:0;border:1px solid #223247;border-radius:10px;background:#0b121a;padding:12px;white-space:pre-wrap;overflow-wrap:anywhere;font:12px/1.55 ui-monospace,monospace;color:#c4d5e8}.preview.diff{color:#dce9f7}.preview.diff .add{color:#7ee2a8}.preview.diff .del{color:#ff9da4}.command-list{display:grid;gap:10px;margin-top:12px}.command-card{border:1px solid #223247;border-radius:10px;background:#0b121a;padding:12px}.command-card.ok{border-left:3px solid var(--green)}.command-card.bad{border-left:3px solid var(--red)}.command-card.pending{border-left:3px solid var(--yellow)}.command-head{display:flex;align-items:center;justify-content:space-between;gap:10px}.command-name{font:12px ui-monospace,monospace;color:#d8e6f5;overflow-wrap:anywhere}.command-status{font-size:11px;color:var(--muted);white-space:nowrap}.command-status.ok{color:var(--green)}.command-status.bad{color:var(--red)}.command-status.pending{color:var(--yellow)}.command-meta{display:flex;flex-wrap:wrap;gap:8px;margin-top:7px;color:var(--muted);font-size:11px}.command-output{margin:10px 0 0;padding:9px;border:1px solid #1b2a3b;border-radius:7px;background:#080e15;white-space:pre-wrap;overflow:auto;max-height:150px;font:12px/1.45 ui-monospace,monospace;color:#bcd0e4}.command-output:empty{display:none}@media(max-width:900px){main{grid-template-columns:1fr}.sidebar{grid-template-columns:1fr 1fr;display:grid}.sidebar .wide{grid-column:1/-1}}@media(max-width:600px){.appbar{height:62px}.tag{display:none}.hero{display:block}.hero-actions{margin-top:16px}.split,.sidebar{display:block}.sidebar .card{margin-bottom:16px}.messages{height:48vh}.msg{max-width:94%}}
</style></head>
<body><header class="appbar"><div class="mark">OC</div><div><div class="brand">OpenClaw Workbench</div><div class="tag">本地可审计 Agent 控制台</div></div><div class="auth"><input id="token" type="password" autocomplete="off" placeholder="控制 Token"><button id="connect" class="primary">连接</button></div><div class="connection"><span id="connectionLight" class="light"></span><span id="connection">未认证</span></div></header>
<main><aside class="sidebar stack"><section class="card workspace wide"><div class="eyebrow">Workbench</div><h1>控制面</h1><p>在本地安全地对话、复核与执行。Token 只保存在当前页面内存中。</p><div id="root" class="root">等待状态…</div><div class="metric"><span>服务状态</span><b id="health">检查中</b></div><div class="metric"><span>人工复核</span><b id="reviews">—</b></div><div class="metric"><span>事件游标</span><b id="latestEvent">—</b></div><button id="refresh" class="ghost" style="width:100%;margin-top:13px">刷新状态</button></section><section class="card wide"><div class="eyebrow">已有会话</div><select id="sessionPicker" class="field" style="min-height:0;margin-top:10px;padding:9px"><option value="">选择会话…</option></select></section><section class="card wide"><div class="eyebrow">运行模式</div><div class="mode-list"><button class="mode active" data-mode="Ask"><i></i><span><b>Ask · 只读</b><br><small>只读分析，不修改文件</small></span></button><button class="mode" data-mode="Plan"><i></i><span><b>Plan</b><br><small>多模型复核方案</small></span></button><button class="mode" data-mode="Code"><i></i><span><b>Code</b><br><small>生成提案，审批后执行</small></span></button></div></section><section class="card wide"><div class="eyebrow">实时事件</div><div id="eventLog" class="event-log">等待事件…</div></section></aside>
<section class="content"><section class="card hero"><div><div class="eyebrow">当前会话</div><h2 id="sessionLabel">尚未创建</h2><p id="modeHint">Ask 不会创建修改提案</p></div><div class="hero-actions"><button id="reviewResume" class="ghost hidden" disabled>恢复会话</button><button id="reviewClose" class="danger hidden" disabled>关闭会话</button><button id="cancel" class="danger" disabled>停止回合</button><button id="newSession" class="primary">新建会话</button></div></section><section class="card"><div id="messages" class="messages"><div class="empty">选择模式并新建会话<br><span class="small">快捷键：⌘/Ctrl + Enter 发送</span></div></div><div id="error" class="hint"></div><form id="composer" class="composer"><textarea id="message" class="field" placeholder="输入任务，例如：请只读分析当前项目的安全风险"></textarea><div class="toolbar"><span class="small" id="count">0 / 32768</span><span class="spacer"></span><span class="kbd">⌘/Ctrl ↵</span><button class="primary" type="submit" id="send">发送</button></div></form></section><div class="split"><section id="planCard" class="card hidden"><div class="row"><div><div class="eyebrow">Plan 复核</div><h3>独立模型分支</h3></div><button id="runPlan" class="primary">开始复核</button></div><textarea id="planQuestion" class="field" style="min-height:75px;margin-top:12px" placeholder="输入需要多模型复核的问题"></textarea><div class="debate-options"><label><input id="debate" type="checkbox"> 启用四阶段 Debate</label><input id="planModels" type="text" value="model-a,model-b" aria-label="Plan 模型列表" placeholder="模型列表，用逗号分隔"><input id="judgeModel" type="text" value="judge" aria-label="裁判模型" placeholder="裁判模型"></div><div id="stageList" class="stage-list" aria-live="polite"><div class="stage">提案</div><div class="stage">质询</div><div class="stage">回应</div><div class="stage">裁判</div></div><div id="planResult" class="plan-output">暂无复核结果</div><div class="toolbar"><span class="small">复核结果不会自动执行</span><span class="spacer"></span><button id="handoffCode" class="primary" disabled>转入 Code 审阅</button><span id="handoffHint" class="small">完成复核后可交接</span></div><div id="planHistory" class="plan-history" aria-label="复核历史"></div></section><section id="proposalCard" class="card hidden"><div class="row"><div><div class="eyebrow">Code 提案</div><h3>执行前必须人工批准</h3></div><div><input id="approvalToken" type="password" autocomplete="off" placeholder="独立审批 Token" style="width:150px;border:1px solid var(--line);border-radius:8px;background:#0a1119;padding:8px 10px"><button id="deny" class="ghost">拒绝</button> <button id="approve" class="primary">批准执行</button></div></div><textarea id="codeInput" class="field" style="min-height:75px;margin-top:12px" placeholder='命令参数 JSON，例如：["pwd"]'></textarea><button id="createProposal" class="primary" style="margin-top:9px">生成命令提案</button><div id="proposalStatus" class="hint">暂无提案</div><div class="toolbar"><button id="viewDiff" class="ghost" disabled>查看 Patch Diff</button></div><div id="proposal" class="proposal">暂无待审批提案</div><div class="toolbar"><button id="loadCommands" class="ghost">查看最近 Terminal 结果</button></div><div id="commandOutput" class="command-list"><div class="small">暂无 Terminal 结果</div></div></section></div><section class="card"><div class="row"><div><div class="eyebrow">Workspace Inspector</div><h3>文件树与只读预览</h3></div><button id="refreshWorkspace" class="ghost">刷新工作区</button></div><div class="workspace-tools"><div id="workspaceTree" class="tree">等待连接…</div><pre id="workspacePreview" class="preview">选择文件查看内容；Patch 提案可查看 Diff。</pre></div></section></section></main><div id="toast" class="toast hidden"></div>
<script nonce="${nonce}">
const state={session:null,mode:'Ask',running:false,token:'',streamAbort:null,proposal:null,lastEvent:0,reconnectTimer:null,workspaceTree:[],expandedDirectories:new Set(),selectedWorkspaceFile:null,reviewPlan:null,stages:{proposal:'待开始',challenge:'待开始',response:'待开始',judge:'待开始'}};const $=s=>document.querySelector(s);const api=async(path,opt={})=>{const headers={'content-type':'application/json',...(opt.headers||{})};if(state.token)headers.authorization='Bearer '+state.token;const r=await fetch(path,{...opt,headers});const text=await r.text();let b={};try{b=JSON.parse(text)}catch{}if(!r.ok){const e=new Error(b.message||b.error||('HTTP '+r.status));e.code=b.error;e.status=r.status;throw e}return b};
function toast(text,bad=false){const el=$('#toast');el.textContent=text;el.className='toast'+(bad?' bad':'');clearTimeout(toast.t);toast.t=setTimeout(()=>el.classList.add('hidden'),3200)}function fail(e){$('#error').textContent=e.message||String(e);toast(e.message||String(e),true)}function escapeHtml(s){return String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function renderStages(){const labels={proposal:'提案',challenge:'质询',response:'回应',judge:'裁判'};$('#stageList').innerHTML=Object.entries(labels).map(([key,label])=>'<div class="stage '+({完成:'done',失败:'failed',进行中:'active'}[state.stages[key]]||'')+'">'+label+' · '+state.stages[key]+'</div>').join('')}
function renderProposal(){const p=state.proposal;const status=p?.action?.status||'';const recovery=p?.recovery;$('#proposalStatus').textContent=p?(recovery?'人工复核 · '+(recovery.reason||'恢复原因'):'提案状态 · '+status):'暂无提案';$('#proposalStatus').style.color=recovery?'var(--red)':status==='awaiting_approval'?'var(--yellow)':status==='verified'?'var(--green)':'var(--muted)';$('#proposal').textContent=p?(recovery?'人工复核：'+(recovery.reason||'恢复原因')+'\n':'')+JSON.stringify(p,null,2):'暂无待审批提案';$('#viewDiff').disabled=p?.action?.type!=='patch';}\nfunction renderPlanHistory(results=[]){const history=$('#planHistory');if(!results.length){history.innerHTML='';return}history.innerHTML=results.slice().reverse().map((result,index)=>'<details '+(index===0?'open':'')+'><summary>'+escapeHtml(result.createdAt||'复核结果')+' · '+(result.debate===true?'Debate':'Plan')+' · '+escapeHtml(result.judgeModel||result.synthesis?.judgeModel||'普通复核')+'</summary><pre>'+escapeHtml(JSON.stringify(result,null,2))+'</pre></details>').join('')}\nasync function loadPlanHistory(){if(!state.session||state.mode!=='Plan')return;try{const b=await api('/v1/sessions/'+state.session.id+'/plan');const results=Array.isArray(b.results)?b.results:[];renderPlanHistory(results);const latest=results.at(-1);if(latest){state.reviewPlan=latest;$('#planResult').textContent=JSON.stringify(latest,null,2);if(latest.debate===true){state.stages={proposal:'完成',challenge:'完成',response:'完成',judge:'完成'}}else{state.stages={proposal:'不适用',challenge:'不适用',response:'不适用',judge:'完成'}}renderStages()}}catch(e){fail(e)}}\nfunction render(){renderStages();renderProposal();$('#handoffCode').disabled=!state.reviewPlan||state.running;$('#handoffHint').textContent=state.reviewPlan?'已载入最近一次复核结果，交接不会自动执行':'完成复核后可交接';document.querySelectorAll('.mode').forEach(b=>b.classList.toggle('active',b.dataset.mode===state.mode));const recovery=state.session?.status==='manual_review';$('#modeHint').textContent=recovery?'当前会话处于人工复核：'+(state.session.recoveryReason||'需要人工确认')+'，发送、Plan、Code 操作均已禁用':state.mode==='Ask'?'Ask 不会创建修改提案':state.mode==='Plan'?'Plan 只做多模型复核，不执行修改':'Code 生成提案，执行仍需独立审批';$('#planCard').classList.toggle('hidden',state.mode!=='Plan');$('#proposalCard').classList.toggle('hidden',state.mode!=='Code');$('#sessionLabel').innerHTML=state.session?'<span>'+state.session.mode+' · '+(recovery?'人工复核':state.session.status==='closed'?'已关闭':'活跃')+'</span><br><span class="session-id">'+escapeHtml(state.session.id)+'</span>':'尚未创建';$('#cancel').disabled=!state.running;$('#reviewResume').classList.toggle('hidden',!recovery);$('#reviewClose').classList.toggle('hidden',!recovery);$('#reviewResume').disabled=!recovery;$('#reviewClose').disabled=!recovery;$('#send').disabled=state.running||recovery;$('#runPlan').disabled=state.running||recovery;$('#createProposal').disabled=recovery;$('#approve').disabled=!state.proposal||recovery;$('#deny').disabled=!state.proposal||recovery}
async function refresh(){try{const b=await api('/v1/status');$('#root').textContent='本地工作区（loopback）';$('#health').textContent='正常';$('#health').style.color='var(--green)';$('#reviews').textContent=(b.persistedState?.sessions?.manualReview||0)+(b.persistedState?.proposals?.manualReview||0);$('#latestEvent').textContent=b.persistedState?.events?.latestSequence??0;$('#connection').textContent='已连接';$('#connectionLight').className='light ok';const s=await api('/v1/sessions');const picker=$('#sessionPicker');picker.innerHTML='<option value="">选择会话…</option>'+s.sessions.map(x=>'<option value="'+escapeHtml(x.id)+'">'+escapeHtml(x.mode+' · '+x.status+' · '+x.id.slice(0,8))+'</option>').join('');if(state.session)picker.value=state.session.id;else if(s.sessions.length){state.session=s.sessions.at(-1);state.mode=state.session.mode;state.stages={proposal:'待开始',challenge:'待开始',response:'待开始',judge:'待开始'};picker.value=state.session.id}const p=await api('/v1/proposals'+(state.session?'?sessionId='+encodeURIComponent(state.session.id):''));const candidates=Array.isArray(p.proposals)?p.proposals:[];state.proposal=candidates.find(x=>x.action?.status==='awaiting_approval')||candidates.find(x=>x.action?.status==='manual_review')||null;renderProposal();render();if(state.session){await loadMessages();if(state.mode==='Plan')await loadPlanHistory();if(state.mode==='Code')await loadCommands()}}catch(e){$('#health').textContent='异常';$('#connection').textContent='连接失败';$('#connectionLight').className='light bad';fail(e)}}
function flattenTree(nodes,depth=0){return nodes.flatMap(node=>{const row={...node,depth};const expanded=node.type==='directory'&&state.expandedDirectories.has(node.path);return [row,...(expanded&&node.children?flattenTree(node.children,depth+1):[])]})}
function renderWorkspaceTree(nodes=[]){const tree=$('#workspaceTree');const flat=flattenTree(nodes);tree.innerHTML=flat.length?flat.map(node=>{const directory=node.type==='directory',expanded=directory&&state.expandedDirectories.has(node.path),selected=!directory&&state.selectedWorkspaceFile===node.path;return '<button type="button" class="tree-item '+(directory?'dir':'')+(selected?' selected':'')+'" style="padding-left:'+(8+node.depth*14)+'px" data-path="'+escapeHtml(node.path)+'" data-type="'+node.type+'"'+(directory?' aria-expanded="'+expanded+'"':selected?' aria-current="true"':'')+'>'+((directory?(expanded?'▾ ':'▸ '):'▤ '))+escapeHtml(node.path.split('/').at(-1))+(node.type==='file'?' <span class="small">'+node.size+' B</span>':'')+'</button>'}).join(''):'<span class="small">工作区为空</span>';tree.querySelectorAll('.tree-item').forEach(item=>item.onclick=()=>{if(item.dataset.type==='directory'){const path=item.dataset.path;state.expandedDirectories.has(path)?state.expandedDirectories.delete(path):state.expandedDirectories.add(path);renderWorkspaceTree(state.workspaceTree)}else loadWorkspaceFile(item.dataset.path)})}
async function loadWorkspaceFile(path){try{const b=await api('/v1/workspace/read?path='+encodeURIComponent(path));state.selectedWorkspaceFile=path;renderWorkspaceTree(state.workspaceTree);$('#workspacePreview').className='preview';$('#workspacePreview').textContent=b.file.content}catch(e){fail(e)}}
async function loadWorkspace(){try{const b=await api('/v1/workspace/tree');state.workspaceTree=b.root?.children||[];renderWorkspaceTree(state.workspaceTree)}catch(e){fail(e)}}
function commandStatus(record){const status=record.status||'unknown';if(['verified'].includes(status))return ['成功','ok'];if(['failed','timed_out','cancelled'].includes(status))return [status==='timed_out'?'超时':status==='cancelled'?'已取消':'失败','bad'];return [status==='executing'?'执行中':status==='claimed'?'等待执行':'人工复核', 'pending']}
function renderCommands(records=[]){const el=$('#commandOutput');if(!records.length){el.innerHTML='<div class="small">暂无 Terminal 结果</div>';return}el.innerHTML=records.map(record=>{const [label,kind]=commandStatus(record);const result=record.result||{};const error=record.error||{};const output=[result.stdout?`标准输出\\n${result.stdout}`:'',result.stderr?`错误输出\\n${result.stderr}`:'',error.message?`错误信息\\n${error.message}`:''].filter(Boolean).join('\\n\\n');return `<article class="command-card ${kind}"><div class="command-head"><span class="command-name">${escapeHtml(Array.isArray(record.command?.argv)?record.command.argv.join(' '):'未知命令')}</span><span class="command-status ${kind}">${label}</span></div><div class="command-meta"><span>退出码：${escapeHtml(result.code??'—')}</span><span>目录：${escapeHtml(result.cwd||'—')}</span><span>${escapeHtml(record.updatedAt||record.claimedAt||'')}</span></div><pre class="command-output">${escapeHtml(output)}</pre></article>`}).join('')}
async function loadCommands(){try{const b=await api('/v1/commands'+(state.session?'?sessionId='+encodeURIComponent(state.session.id):''));renderCommands(Array.isArray(b.commands)?b.commands:[])}catch(e){fail(e)}}
async function loadProposalDiff(){if(!state.proposal?.action?.id)return;try{const b=await api('/v1/proposals/'+state.proposal.action.id+'/diff');const preview=$('#workspacePreview');preview.className='preview diff';preview.innerHTML=escapeHtml(b.diff.patch).split('\n').map(line=>'<span class="'+(line.startsWith('+')&&!line.startsWith('+++')?'add':line.startsWith('-')&&!line.startsWith('---')?'del':'')+'">'+line+'</span>').join('\n')}catch(e){fail(e)}}
async function loadMessages(){if(!state.session)return;const b=await api('/v1/sessions/'+state.session.id+'/messages');$('#messages').innerHTML=b.messages.length?b.messages.map(m=>'<div class="msg '+(m.role==='user'?'user':'assistant')+'"><b>'+m.role+'</b>'+escapeHtml(typeof m.content==='string'?m.content:JSON.stringify(m.content,null,2))+'</div>').join(''):'<div class="empty">暂无消息</div>';$('#messages').scrollTop=$('#messages').scrollHeight;await loadPlanHistory()}
function logEvent(ev){const log=$('#eventLog');if(log.textContent==='等待事件…')log.textContent='';const line=document.createElement('div');line.className='event';line.innerHTML='<time>'+new Date(ev.createdAt||Date.now()).toLocaleTimeString()+'</time>'+escapeHtml(ev.type);log.prepend(line);while(log.children.length>8)log.lastElementChild.remove()}
function connectEvents(){if(state.streamAbort)state.streamAbort.abort();clearTimeout(state.reconnectTimer);if(!state.token)return;state.streamAbort=new AbortController();const after=state.lastEvent;fetch('/v1/events/stream?after='+after,{headers:{authorization:'Bearer '+state.token},signal:state.streamAbort.signal}).then(async r=>{if(r.status===409){let body={};try{body=await r.json()}catch{}if(body.error==='EVENT_CURSOR_EXPIRED'&&Number.isSafeInteger(body.earliestSequence)){state.lastEvent=Math.max(0,body.earliestSequence-1);$('#connection').textContent='事件历史已过期，正在从最早位置恢复';$('#connectionLight').className='light';state.reconnectTimer=setTimeout(connectEvents,50);return}throw new Error(body.message||'事件流游标恢复失败')}if(!r.ok)throw new Error('事件流认证失败');$('#connection').textContent='事件流已连接';$('#connectionLight').className='light ok';const reader=r.body.getReader(),decoder=new TextDecoder();let buffer='';while(true){const x=await reader.read();if(x.done)break;buffer+=decoder.decode(x.value,{stream:true});const chunks=buffer.split('\n\n');buffer=chunks.pop();for(const chunk of chunks){const id=chunk.split('\n').find(v=>v.startsWith('id: '));const line=chunk.split('\n').find(v=>v.startsWith('data: '));if(line){const ev=JSON.parse(line.slice(6));if(Number(id?.slice(4))<=state.lastEvent)continue;state.lastEvent=Number(id?.slice(4))||state.lastEvent;logEvent(ev);if(ev.sessionId===state.session?.id){if(ev.type.startsWith('plan.stage.')){const stage=ev.data?.stage;if(stage)state.stages[stage]=ev.data.status==='started'?'进行中':ev.data.status==='completed'?'完成':ev.data.status==='failed'?'失败':ev.data.status;render()}if(['chat.completed','turn.cancel.requested','plan.completed'].includes(ev.type)){state.running=false;render();loadMessages()}}}}}}).catch(e=>{if(e.name!=='AbortError'){$('#connection').textContent='事件重连中';$('#connectionLight').className='light';state.reconnectTimer=setTimeout(connectEvents,1500)}})}
$('#sessionPicker').onchange=async e=>{if(!e.target.value)return;try{const s=(await api('/v1/sessions')).sessions.find(x=>x.id===e.target.value);if(s){state.session=s;state.mode=s.mode;state.reviewPlan=null;state.stages={proposal:'待开始',challenge:'待开始',response:'待开始',judge:'待开始'};render();await loadMessages();if(state.mode==='Plan')await loadPlanHistory()}}catch(err){fail(err)}};document.querySelectorAll('.mode').forEach(b=>b.onclick=()=>{if(state.running)return toast('当前回合结束后再切换模式',true);state.mode=b.dataset.mode;render()});$('#connect').onclick=()=>{state.token=$('#token').value.trim();if(!state.token)return fail(new Error('请输入控制 Token'));refresh();connectEvents()};$('#refresh').onclick=refresh;$('#refreshWorkspace').onclick=loadWorkspace;$('#newSession').onclick=async()=>{try{state.session=(await api('/v1/sessions',{method:'POST',body:JSON.stringify({mode:state.mode,actor:'control-panel'})})).session;state.reviewPlan=null;render();await loadMessages();toast('会话已创建')}catch(e){fail(e)}};$('#cancel').onclick=async()=>{if(!state.session||!state.running)return;try{await api('/v1/sessions/'+state.session.id+'/cancel',{method:'POST'});toast('已发送停止请求')}catch(e){fail(e)}};async function reviewSession(decision){if(!state.session||state.session.status!=='manual_review')return;try{state.session=(await api('/v1/sessions/'+state.session.id+'/review',{method:'POST',body:JSON.stringify({decision,reviewer:'control-panel:user'})})).session;render();await refresh();toast(decision==='resume'?'会话已恢复；不会重放中断回合':'会话已关闭；不会重放中断回合')}catch(e){fail(e)}}$('#reviewResume').onclick=()=>reviewSession('resume');$('#reviewClose').onclick=()=>reviewSession('close');
async function send(){if(!state.session)return fail(new Error('请先新建会话'));const text=$('#message').value.trim();if(!text)return;$('#message').value='';state.running=true;$('#error').textContent='';render();try{const b=await api('/v1/sessions/'+state.session.id+'/messages',{method:'POST',body:JSON.stringify({message:text})});state.session=b.session;await loadMessages();toast('回合完成')}catch(e){state.running=false;fail(e)}finally{state.running=false;render()}}$('#composer').onsubmit=e=>{e.preventDefault();send()};$('#message').oninput=e=>$('#count').textContent=e.target.value.length+' / 32768';$('#message').onkeydown=e=>{if((e.metaKey||e.ctrlKey)&&e.key==='Enter'){e.preventDefault();send()}};
$('#handoffCode').onclick=async()=>{if(!state.reviewPlan||state.running)return;try{const review=state.reviewPlan;state.session=(await api('/v1/sessions',{method:'POST',body:JSON.stringify({mode:'Code',actor:'control-panel:plan-handoff'})})).session;state.mode='Code';const decision=review.rounds?.verdict?.text||review.synthesis||review;$('#message').value='请基于以下已完成人工可审阅的 Plan 结果，先说明拟议变更与风险，再等待我明确确认后生成 Code 提案。不得直接执行。\\n\\n'+JSON.stringify(decision,null,2);$('#count').textContent=$('#message').value.length+' / 32768';render();await loadMessages();toast('已转入 Code 审阅，尚未生成或执行提案')}catch(e){fail(e)}};$('#viewDiff').onclick=loadProposalDiff;$('#loadCommands').onclick=loadCommands;$('#createProposal').onclick=async()=>{if(!state.session)return fail(new Error('请先创建 Code 会话'));try{const argv=JSON.parse($('#codeInput').value);const b=await api('/v1/proposals/command',{method:'POST',body:JSON.stringify({sessionId:state.session.id,argv})});state.proposal=b.proposal;$('#proposal').textContent=JSON.stringify(state.proposal,null,2);render();if(state.proposal.action?.type==='patch')loadProposalDiff();toast('提案已生成')}catch(e){fail(e)}};$('#approve').onclick=async()=>{if(!state.proposal)return;try{await api('/v1/proposals/'+state.proposal.action.id+'/approve',{method:'POST',headers:{'x-approval-token':$('#approvalToken').value.trim()},body:JSON.stringify({actionHash:state.proposal.action.actionHash})});state.proposal=null;await refresh();toast('提案已执行')}catch(e){fail(e)}};$('#deny').onclick=async()=>{if(!state.proposal)return;try{await api('/v1/proposals/'+state.proposal.action.id+'/deny',{method:'POST',headers:{'x-approval-token':$('#approvalToken').value.trim()}});state.proposal=null;await refresh();toast('提案已拒绝')}catch(e){fail(e)}};$('#runPlan').onclick=async()=>{if(!state.session)return fail(new Error('请先创建 Plan 会话'));const question=$('#planQuestion').value.trim();if(!question)return fail(new Error('请填写复核问题'));const models=$('#planModels').value.split(',').map(x=>x.trim()).filter(Boolean);if(models.length<2)return fail(new Error('至少填写两个模型'));const debate=$('#debate').checked;const judgeModel=$('#judgeModel').value.trim();state.stages={proposal:'待开始',challenge:'待开始',response:'待开始',judge:'待开始'};try{state.running=true;render();const b=await api('/v1/sessions/'+state.session.id+'/plan',{method:'POST',body:JSON.stringify({question,models,...(debate?{debate:true,judgeModel}: {})})});state.reviewPlan=b;$('#planResult').textContent=JSON.stringify(b,null,2);toast(debate?'Debate 复核完成':'复核完成')}catch(e){fail(e)}finally{state.running=false;render()}};
refresh();render();loadWorkspace();connectEvents();
</script></body></html>`;
}
